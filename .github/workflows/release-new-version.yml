name: Create a New Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: 'Version component to bump'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
          - rc

run-name: "Release ${{ inputs.bump }} bump on ${{ github.ref_name }}"

permissions:
  contents: write

jobs:
  release:
    uses: valkyrjaio/.github/.github/workflows/_create-php-release.yml@396a3e938cf1c222f11e98afe54612d9170c0557
    secrets: inherit
    with:
      bump: ${{ inputs.bump }}
      dependencies: |
        [
          {"name": "Root Packages", "command": "root-outdated-check"},
          {"name": "PHPStan",  "command": "phpstan-outdated-check"}
        ]
      info-class-path: src/PhpStan/Constant/PhpStanInfo.php
      info-class-name: PhpStanInfo

  update-refs:
    name: Update Workflow Refs
    needs: release
    uses: valkyrjaio/.github/.github/workflows/_update-workflow-refs.yml@396a3e938cf1c222f11e98afe54612d9170c0557
    secrets: inherit
    with:
      source-repo: ${{ github.event.repository.name }}
